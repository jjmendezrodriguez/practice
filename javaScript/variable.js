// variables

// v = variable
let count = 5;
// v.name valor

// camelcase se usa para separar texto
maxNumber;
// es como dejar un espacio max number

let age = 1; // let count igual a 0
// usando let puedes cambiar el valor de la variable
age = 5;

// para incrementar
age = age + 1;
// para reducir usa - u otros como *, / ect..
count = count * 2;
console.log(count);

// for print in .js use:
// se usa console donde se ve el sesultado de hay su nombre. y log por registro
// es como decir imprime el registro de la consola que es (lo que esta aqui dentro)
console.log(age);
// antes de imprimir el valor debe estar antes. la cpu lee up to down.
// en .js debe terminar con ;
