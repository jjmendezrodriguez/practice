// variables
// son sensibles a simbolos y letras mayusculas.
// v = variable
let count = 5;
// v.name = valor de dataType for more info.

/* In .js para dejar espacio es con mayuscula no _ de llama:
camelcase = maxNumber is like dejar espacio entre textos */
maxNumber = "numero maximo";
// es como dejar un espacio 'max number' (en .js no puedes usar _, -)

let age = 1; // let count igual a 1
// usar 'let' declara una variable que puedes puede cambiar el valor
age = 5;

// para incrementar
age = age + 1;
// Para reducir usa resta (-) u otros simbolos como *, / ect..
count = count * 2;
// contara ^ de 2 en 2
console.log(count);

/* For print in .js use console = donde se ve el resultado,
de hay su nombre. log por registro, es como decir imprime el registro
en la consola que es ('inside parentheses') */
console.log("texto");
/* En una variable el valor de la variable debe estar antes del comando.
La cpu lee up to down and left to right. Si no es variable debe usar
"texto entre comillas" para numeros (4) no se usa. En .js debe 
terminar con punto y coma (;) */
