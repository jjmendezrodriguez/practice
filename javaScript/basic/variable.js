// variables
// son sensibles a simbolos y letras mayusculas.
// v = variable
let count = 5;
// v.name = valor

// camelcase se usa para separar texto
maxNumber = "numero maximo";
// es como dejar un espacio 'max number' en .js no puedes usar _, -

let age = 1; // let count igual a 1
// usar 'let' declara una variable que puedes puede cambiar el valor
age = 5;

// para incrementar
age = age + 1;
// para reducir usa resta (-) u otros simbolos como *, / ect..
count = count * 2;
console.log(count);

/* For print in .js se usa console, por donde se ve el resultado,
de hay su nombre. log por registro, es como decir imprime el registro
en la consola que es ('lo que esta aqui dentro') */
console.log("texto");
/* si es una variable antes de imprimir, el valor de la variable debe
estar antes. La cpu lee up to down. 
Si no es variable debe usar "texto entre comillas" para numeros (4)
no se usa. En .js debe terminar con punto y coma (;) */
